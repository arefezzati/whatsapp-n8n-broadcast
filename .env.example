# =====================================================
# WhatsApp Bridge - Environment Variables
# =====================================================
# Bu dosyayı .env olarak kopyalayıp düzenleyin

# =====================================================
# BASIC CONFIGURATION
# =====================================================

# Port (varsayılan: 3001)
PORT=3001

# Base Path (reverse proxy için, örn: /whatsapp-web)
# Render.com'da boş bırakın
BASE_PATH=

# =====================================================
# LOGGING
# =====================================================

# File Logging (true/false)
# - Localhost: true (logs klasörüne yazar)
# - Render.com: false (sadece console, Render loglarını gösterir)
FILE_LOGS=true

# =====================================================
# NGROK TUNNEL (Sadece localhost geliştirme için!)
# =====================================================

# Ngrok kullan (true/false)
# - Localhost: true (dışarıdan erişim için)
# - Render.com: false (zaten public URL var)
USE_NGROK=false

# Ngrok Token (tunnel için - opsiyonel)
# https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTH_TOKEN=your_ngrok_token_here

# =====================================================
# BROWSER (Opsiyonel)
# =====================================================

# Puppeteer Chrome Path (otomatik bulur, değiştirme)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome

# =====================================================
# RENDER.COM DEPLOYMENT NOTLARI
# =====================================================
# 
# 1. Environment Variables (Render Dashboard):
#    PORT=3001
#    FILE_LOGS=false
#    USE_NGROK=false
#
# 2. Persistent Disk (Önemli!):
#    - Add Disk: 1GB ($1/ay)
#    - Mount Path: /data/whatsapp-session
#    - wweb-session klasörünü buraya taşı
#
# 3. Build Command:
#    npm install
#
# 4. Start Command:
#    npm start
#
# 5. Health Check Path:
#    /health
#
# =====================================================
